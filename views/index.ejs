<h1 class="mb-4">Welcome to the Restaurant Reservation System!</h1>
<% if (user) { %>
  <p class="lead">You are logged in as <strong><%= user.name %></strong> (<%= user.email %>).</p>
  <p>Please use the navigation above to explore the app.</p>
<% } else { %>
  <p class="lead">Please <a href="/auth/google">login</a> to manage tables and reservations.</p>
<% } %>
